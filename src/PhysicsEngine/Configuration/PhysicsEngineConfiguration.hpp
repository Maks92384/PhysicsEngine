#pragma once

namespace physicsConf {
    static const sf::Vector3f defaultHitbox = {2, 3, 4};

    // 3D engine integration configuration

    static const float distanceScale = 100.0f; // How many units in the 3D engine is one meter
}
